const express = require('express');

const {

} = require('../controllers/auctionController');

const router = express.Router();

/*
    ! ON HOLD FOR NOW
    * Reason - I believe we will be using websockets for auctioning, so idk if we will need restful api for this
*/

module.exports = router;